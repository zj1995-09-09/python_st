# encoding: utf-8
# @author: MrZhou
# @file: 16列表.py
# @time: 2023/2/16 11:10
# @desc:
list1 = ['physics', 'chemistry', 1997, 2000]
list2 = [1, 2, 3, 4, 5, 6, 7]

# 可以对列表的数据进行修改或更新，也可以用append()方法来添加列表项
# list = []
# list.append('Google')
# list.append('Runoob')
# print(list)
#
# python列表脚本操作符 列表对+和*的操作符与字符串相似。+号用于组合列表，*用于重复列表
# len([1,2,3])
#
# len([1, 2, 3])	3	长度
# [1, 2, 3] + [4, 5, 6]	[1, 2, 3, 4, 5, 6]	组合
# ['Hi!'] * 4	['Hi!', 'Hi!', 'Hi!', 'Hi!']	重复
# 3 in [1, 2, 3]	True	元素是否存在于列表中
# for x in [1, 2, 3]: print x,	1 2 3	迭代

# python 列表函数&方法
# cmp(list1,list2)比较两个列表的元素
# len(list) 列表元素的个数
# max(list)
# min(list)
# list(seq) 将元祖转换为列表
#
# 包含以下方法
# list.append() 在列表末尾添加新的对象
# list.count() 统计某个元素在列表中出现的次数
# list.extend(seq) 在列表末尾一次性追加另一个序列中的多个值 用新列表扩展原来的列表
# list.index() 从列表中找出某个值第一个匹配项的索引位置
# list.insert(index,object)将对象插入列表
# list.pop([index=-1]) 移除列表中的一个元素,默认是最后一个元素，并且返回该元素的值
# list.remove(object)移除列表中某个值的第一个匹配项
# list.reverse()反向列表中元素
# list.sort()对原列表进行排序
